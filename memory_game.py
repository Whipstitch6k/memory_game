#need a deck object, array of values
  #constructor will initialize based on a defined grid size, pairs using letters to start
#need a grid to be defined from deck after shuffle

#need a game class
  #gamestate variable (ongoing, finished), while gamestate == ongoing
  #player object containing score
  #turn method